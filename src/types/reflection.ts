export interface Reflection {
  id: string;
  date: string;
  sessionId: string | null;
  questId: string | null;
  questTitle: string | null;
  beforeEmotion: string;
  afterEmotion: string;
  notes: string;
  createdAt: string;
}
