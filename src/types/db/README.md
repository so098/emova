# DB Row 타입

Supabase `select()` 결과를 타입 캐스팅할 때 사용하는 타입들.

DB 컬럼(`snake_case`) → 도메인 타입(`camelCase`) 변환 시 중간 타입 역할.

## 왜 수동 관리?

`supabase gen types`로 자동 생성할 수 있지만, 현재 테이블 수가 적고(10개) 필요한 필드만 골라 쓰는 구조라 수동이 더 간결함.

테이블이 20개 이상으로 늘거나 스키마 변경이 잦아지면 자동 생성 전환 검토.
